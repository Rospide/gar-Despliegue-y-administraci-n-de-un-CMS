# templates

Esta carpeta contiene plantillas usadas por Ansible durante el despliegue.

Contenido:

- `wp-config.php.j2`: plantilla Jinja2 para generar el archivo `wp-config.php` de WordPress.

Las plantillas permiten generar configuraciones adaptadas a los valores definidos en los playbooks y en el inventario.
